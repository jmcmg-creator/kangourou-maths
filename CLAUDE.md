# CLAUDE.md

Les consignes de ce dépôt tiennent dans un seul fichier, partagé par tous les agents
(Codex lit `AGENTS.md`, Claude Code lit ce fichier-ci). Ne rien écrire ici : tout se
met à jour dans `AGENTS.md`.

@AGENTS.md
